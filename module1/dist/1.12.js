"use strict";
{
    // 
    // function materParamitter(value: unknown){
    //     if(typeof value === 'number'){
    //        const convertedTime = (value * 1000) / 3600;
    //        console.log(`The Answer id ${convertedTime} mth^-1`);
    //     }
    //     else if(typeof value ==='string'){
    //         const [result, symbols] = value.split(' ')
    //         const convertedTime = (parseFloat(result) * 1000) / 3600;
    //         console.log(`The Answer id ${convertedTime} mth^-1`);
    //     }
    //     else{
    //         console.log('Wrong Input');
    //     }
    // }
    // materParamitter(null)
    const throwMessage = (msg) => {
        throw new Error(msg);
    };
    throwMessage('Errror hoilo kn ? bujte pari nah vai');
    // 
}
